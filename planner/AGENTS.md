# Planner-local notes

This directory is the planner agent's local workspace inside `$PLANROOT`.

Project-wide plan rules, helper commands, tree invariants, and file formats are
defined by `$PLANROOT/AGENTS.md`.

No additional planner-specific rules are currently needed here.
